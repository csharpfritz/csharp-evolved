### Optional newer-version capability (C# 9+)

The baseline examples on this page are C# 3.0/NETFx 3.5 compatible.

Target-typed `new` is newer syntax and should be treated as an optional modernization:

```csharp
Dictionary<string, List<decimal>> trailingOrders = new();
```
